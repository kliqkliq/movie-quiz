<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MenuButton">
        <item name="android:fontFamily">@font/agent_orange</item>
        <item name="android:padding">@dimen/menu_button_padding</item>
        <item name="android:textColor">@color/menu_font_color</item>
        <item name="android:textSize">@dimen/menu_button_text_size</item>
    </style>

</resources>
